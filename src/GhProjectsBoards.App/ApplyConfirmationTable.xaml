<UserControl
    x:Class="GhProjectsBoards.App.ApplyConfirmationTable"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GhProjectsBoards.App">
    <UserControl.Resources>
        <CollectionViewSource x:Name="GroupedRows" IsSourceGrouped="True" />
    </UserControl.Resources>
    <Grid RowSpacing="4">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid x:Name="Header" Padding="12,4,12,4" ColumnSpacing="8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="32"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <CheckBox x:Name="SelectAll" AutomationProperties.AutomationId="ApplySelectAll"
                      AutomationProperties.Name="候補をすべて選択または解除" IsThreeState="True" MinWidth="0" Click="SelectAllClicked"/>
            <Grid x:Name="ColumnHeader" Grid.Column="1" ColumnSpacing="12" VerticalAlignment="Center"/>
        </Grid>
        <ListView x:Name="Rows" Grid.Row="1" SelectionMode="Multiple" SelectionChanged="SelectionChanged"
                  AutomationProperties.AutomationId="ApplyTargetRows" AutomationProperties.Name="反映候補と変更前後"
                  ItemsSource="{Binding Source={StaticResource GroupedRows}}" HorizontalContentAlignment="Stretch"
                  ScrollViewer.HorizontalScrollMode="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemsPanel><ItemsPanelTemplate><ItemsStackPanel AreStickyGroupHeadersEnabled="True"/></ItemsPanelTemplate></ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ApplyConfirmationRow">
                    <ContentPresenter Content="{x:Bind Content, Mode=OneTime}" HorizontalContentAlignment="Stretch"/>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate x:DataType="local:ApplyConfirmationGroup">
                            <Border Background="{ThemeResource LayerFillColorDefaultBrush}" Padding="12,6">
                                <TextBlock Text="{x:Bind Name}" Style="{StaticResource BodyStrongTextBlockStyle}" TextWrapping="Wrap"/>
                            </Border>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>
    </Grid>
</UserControl>
