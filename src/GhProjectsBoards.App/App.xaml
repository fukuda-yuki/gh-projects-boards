<Application
    x:Class="GhProjectsBoards.App.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:local="using:GhProjectsBoards.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <controls:XamlControlsResources />
            </ResourceDictionary.MergedDictionaries>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="SheetChangedBrush" Color="#FFF5DB"/>
                    <SolidColorBrush x:Key="SheetSelectionBrush" Color="#E6F0FA"/>
                    <SolidColorBrush x:Key="SheetSelectionStrokeBrush" Color="{ThemeResource SystemAccentColorDark1}"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="SheetChangedBrush" Color="#3D3525"/>
                    <SolidColorBrush x:Key="SheetSelectionBrush" Color="#253D52"/>
                    <SolidColorBrush x:Key="SheetSelectionStrokeBrush" Color="{ThemeResource SystemAccentColorLight2}"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <SolidColorBrush x:Key="SheetChangedBrush" Color="{ThemeResource SystemColorWindowColor}"/>
                    <SolidColorBrush x:Key="SheetSelectionBrush" Color="{ThemeResource SystemColorWindowColor}"/>
                    <SolidColorBrush x:Key="SheetSelectionStrokeBrush" Color="{ThemeResource SystemColorHighlightColor}"/>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
            <DataTemplate x:Key="GanttRowTemplate">
                <local:GanttRowPresenter HorizontalContentAlignment="Stretch"/>
            </DataTemplate>
            <Style x:Key="GanttSurfaceStyle" TargetType="Grid">
                <Setter Property="Background" Value="{ThemeResource ApplicationPageBackgroundThemeBrush}"/>
            </Style>
            <Style x:Key="ProjectViewsStyle" TargetType="SelectorBar">
                <Setter Property="Background" Value="{ThemeResource ApplicationPageBackgroundThemeBrush}"/>
            </Style>
            <Style x:Key="GanttWorkingDayStyle" TargetType="Rectangle">
                <Setter Property="Fill" Value="{ThemeResource LayerFillColorDefaultBrush}"/>
                <Setter Property="Stroke" Value="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                <Setter Property="StrokeThickness" Value="0.5"/>
            </Style>
            <Style x:Key="GanttRestDayStyle" TargetType="Rectangle" BasedOn="{StaticResource GanttWorkingDayStyle}">
                <Setter Property="Fill" Value="{ThemeResource ControlFillColorSecondaryBrush}"/>
            </Style>
            <Style x:Key="GanttAutoBarStyle" TargetType="Rectangle">
                <Setter Property="Fill" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
                <Setter Property="Stroke" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
                <Setter Property="StrokeThickness" Value="1"/>
            </Style>
            <Style x:Key="GanttManualBarStyle" TargetType="Rectangle" BasedOn="{StaticResource GanttAutoBarStyle}">
                <Setter Property="Fill" Value="{ThemeResource ControlFillColorDefaultBrush}"/>
            </Style>
            <Style x:Key="GanttLinkStyle" TargetType="Polyline">
                <Setter Property="Stroke" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
                <Setter Property="StrokeThickness" Value="1"/>
            </Style>
            <Style x:Key="GanttEndpointStyle" TargetType="Line">
                <Setter Property="Stroke" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
                <Setter Property="StrokeThickness" Value="1"/>
            </Style>
            <Style x:Key="SheetHeaderStyle" TargetType="Grid">
                <Setter Property="Background" Value="{ThemeResource SolidBackgroundFillColorSecondaryBrush}"/>
            </Style>
            <Style x:Key="SheetCellStyle" TargetType="Border">
                <Setter Property="BorderBrush" Value="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                <Setter Property="Background" Value="{ThemeResource SolidBackgroundFillColorBaseBrush}"/>
            </Style>
            <Style x:Key="SheetSelectedCellStyle" TargetType="Border" BasedOn="{StaticResource SheetCellStyle}">
                <Setter Property="Background" Value="{ThemeResource SheetSelectionBrush}"/>
            </Style>
            <Style x:Key="SheetChangedCellStyle" TargetType="Border" BasedOn="{StaticResource SheetCellStyle}">
                <Setter Property="Background" Value="{ThemeResource SheetChangedBrush}"/>
            </Style>
            <Style x:Key="SheetProblemCellStyle" TargetType="Border" BasedOn="{StaticResource SheetCellStyle}">
                <Setter Property="Background" Value="{ThemeResource SystemFillColorCautionBackgroundBrush}"/>
            </Style>
            <Style x:Key="SheetSelectionFrameStyle" TargetType="Border">
                <Setter Property="BorderBrush" Value="{ThemeResource SheetSelectionStrokeBrush}"/>
            </Style>
            <Style x:Key="SheetEditingFrameStyle" TargetType="Border">
                <Setter Property="BorderBrush" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
            </Style>
            <Style x:Key="SheetFillHandleStyle" TargetType="Button">
                <Setter Property="Background" Value="{ThemeResource SheetSelectionStrokeBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource SolidBackgroundFillColorBaseBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="0"/>
            </Style>
            <Style x:Key="SheetChoiceCellStyle" TargetType="Button">
                <Setter Property="UseSystemFocusVisuals" Value="False"/>
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}"
                                              Padding="{TemplateBinding Padding}" Foreground="{TemplateBinding Foreground}"
                                              HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Background="Transparent"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="SheetDetailsStyle" TargetType="Border">
                <Setter Property="BorderBrush" Value="{ThemeResource DividerStrokeColorDefaultBrush}"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
