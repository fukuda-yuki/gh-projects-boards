## Post-#68 corrective increment — review handoff, acceptance remains open

This increment starts from integrated `ab292d5`. Product corrections are committed at `a776073`; final ordinary/driver measurements use `948dd53` with identical product source. Later commits add evidence. Two independent combined-source reviews, reproductions, dispositions, frozen measurements and isolated Fresh/Weekly reevaluation roots are retained in [the receipt](https://github.com/fukuda-yuki/gh-projects-boards/tree/5865732411f6b085bf24d5d85846adc519b58d5d/tests/regression-evidence/issue65-post68).

Resolved surviving findings: rejected first-checkpoint retry with recovery bytes preserved, protected-baseline/verified identity and per-edge dependency collision, conflicting canonical Project-item appearances, Summary containment, raw rollup detail, unknown effort, real cutoff heading and selected task/attribution continuity. Duplicate Gantt appearances no longer throw. Cold projection mutation and speculative lazy-editor focus failure were not reproduced; pending text remains separate from committed calculation by contract. No checkpoint downgrade, new Summary expansion, forced GC, save suppression, priority change or gh concurrency was introduced.

**#65 performance remains FAIL.** All three cold and three warm corrected runs retain application-side viewport delays with independent pixel evidence. Typed native-value/UIA p95 is 57.56–79.06 ms across the measured title/NUMBER phases; maximum scroll callback intervals are 260.91–353.56 ms. All 182 >=100 ms scroll callback intervals are classified, with 36 still unexplained and additional correlated visible/application delays. These counts do not represent physical scanout. Source-specific runtime/profile/span/save evidence and the unexplained portion are in [performance.md](https://github.com/fukuda-yuki/gh-projects-boards/blob/5865732411f6b085bf24d5d85846adc519b58d5d/tests/regression-evidence/issue65-post68/performance.md). Native retention still fails 78 against <=65 on baseline and corrected source. The Gantt recalculation maximum is 1,298.9222 ms against <=1,000 ms.

Execution evidence remains separate: Core 659/659 before the final ordering-only fix, then 40/40 affected checks; UI 84 pass/4 fail in 88, then 3/3 corrected operations while timing remains failed; four additional combined regressions pass; ordinary fake-gh planning/Gantt/restart 2/2; final physical IME/save-retry, thumb/distant/horizontal-return and trace-disabled companions pass state/persistence checks. The final retention case fails in a separate host. There is no synthetic all-pass total.

The #61 focused ordinary job is ready for isolated human reevaluation, which is NOT RUN. #64 stays contained as `Summary（準備中）`; complete Unit D and expansion remain held. P1/P2, other display/High Contrast/accessibility environments, physical scanout, broader recovery, #51 throughput, release and live GitHub retain their owners. Neither this increment nor a later merge closes #61/#64/#65.

During implementation, automatic approval review rejected the push before execution (`Pushing to a remote is denied; do it manually.`). No agent-created PR/CI/main integration or Issue update is claimed for that attempt. This prepared note is for publication after the user completes the manual handoff and supplies the actual PR URL; the prior denial remains historical evidence.
