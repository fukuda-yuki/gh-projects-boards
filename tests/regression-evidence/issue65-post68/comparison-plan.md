# Prospective stabilization comparison

Owner: #65 post-merge assignment, with #64 containment/correctness and #61 FLOW preservation. Baseline source is integrated main `ab292d50f02d4e373871c48f408ce1151bf7a169`. The final candidate will be frozen and hashed before the six acceptance measurements. Intermediate diagnostic attempts remain separate.

Use the retained Test-SustainedInput driver and the same frozen fixture producer on both sources. Each execution has a fresh synthetic root/process: 1,000 tasks, 20 people, six fields, mixed plans and historical actuals, an offscreen pending buffer and Undo history. Record the actual initial checkpoint size and producer/Core, driver, App/Core/executable hashes. Standard workload is 20 seconds title, 20 seconds NUMBER and 20 seconds wheel/horizontal motion; warm adds ten unmeasured seconds. Retain 1080 by 760 physical window dimensions and record actual display, DPI and viewport. Three independent cold and three independent warm final-candidate executions are required. Baseline cold/warm runs establish current-source reproduction. Profile separately and keep profiling overhead out of comparative measurements. Do not run builds or other performance experiments concurrently with measurements.

Native-value/UIA readback including observer cost, rendering callbacks, timestamped independent GDI captures and durable checkpoint saving are distinct boundaries. Typed p95 remains <=100 ms. Enumerate every >=100 ms rendering interval during commanded motion, correlate input timestamps, requested versus observed viewport movement, independently captured row identities/pixels, UI spans and separate runtime GC/allocation/finalizer samples. Record unexplained intervals; callback gaps alone neither prove nor clear visible freezes. A reproduced or unexplained application-caused freeze keeps performance open. Physical scanout remains NOT RUN. No causal speedup percentage from unmatched or incomplete old traces.

Run the retained thumb/distant-edit/horizontal/return diagnostic and representative ordinary planning/Gantt/restart journeys on the final candidate. Native input and save failure/retry must preserve current and offscreen pending text, original history/attribution and Undo. Assert terminal drain, no missing/dropped trace sequences and normal-close durable readback. Retain all failures/outliers separately. Human reevaluation, other display/accessibility environments, broader recovery/Apply throughput and complete Summary acceptance remain separately owned, never inferred from these runs.

## Observer attribution amendment, before light-observer/candidate runs

The integrated baseline full-observer cold/warm runs completed with drained traces. A separate baseline runtime profile found substantial CaptureDiagnosticState visual-tree walks and WinRT finalization during scrolling. Full diagnostics therefore cannot be treated as free. A trace-disabled baseline repetition completed with independent frames and still contains delayed viewport response. An earlier trace-disabled attempt failed native-value readback and did not reach scrolling; retain it separately (focus interference is suspected, not proven).

Add an explicit full/light/off trace-detail selection to the retained driver. Light keeps input, viewport, dispatcher spans, render callbacks, allocation/GC counters, saving and terminal drain, but disables the intrusive full visual-tree census. Full retains that census for investigation, with its time now separately attributed. Off keeps the independent capture/input driver only. These observer variants are not an application speedup. Freeze an observer-only baseline before matched final measurements if light is selected; compare identical observer, driver and producer settings. A correctness candidate light run is exploratory, not the final six-run acceptance. Preserve the original full-observer runs and profile.
