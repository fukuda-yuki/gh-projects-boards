# Source, design and evidence review

User task: move to a distant or previously visited sheet viewport, keep the same pending editor/identity, and continue ordinary native editing. Review used AGENTS.md, DESIGN.md, tests/README.md and the current post-69 assignment, with the installed WinUI development, design, code-review and UI-testing guidance. This is the implementing agent's review, not an independent human or second-agent acceptance.

The two-file production diff sets ItemsStackPanel CacheLength to zero and explains why. The [public CacheLength contract](https://learn.microsoft.com/en-us/windows/windows-app-sdk/api/winrt/microsoft.ui.xaml.controls.itemsstackpanel.cachelength?view=windows-app-sdk-1.8) defines an offscreen buffer in viewport multiples. The responsible work is independently observed: 18 realized rows/54 newly constructed native TextBoxes around a viewport displaying 12 rows; the shorter cache causes 13 managed realizations in the matched destination and removes seven creations at the matched return. Native panel anchoring may still realize an extra row; this does not promise an exact visible-row count. Actual scrolling still exceeds the gate, so less realization is not a general latency-pass claim.

The change leaves the separate 64-row dormant cache, protected focused/pending rows, event detach/disposal, native template/input, field identity and GitHub/storage boundaries intact. No binding, dependency, API contract, private framework hook, product timer or forced GC was introduced. No tests, producer, thresholds, sleeps, diagnostic classifications or performance workload were modified. Existing source assertions remain; no historical failing case was deleted or reclassified.

Inspected retained Light/Dark actual-control pixels show visible title text, native clear button, identity and selection/focus outlines. Focused return keeps the pending title and caret; the narrow test host's existing clipping/ellipsis is not treated as whole-window UX acceptance. Ordinary native-bar pixels show row 1,000 visible with a newly typed title and the separate pending NUMBER value. Final cold-01 before/after captures show the old viewport remaining and then the expected row-241 destination; those stills support their timed capture brackets, not smooth motion or physical scanout. Representative pixels and provenance are linked from `pixels.json`. High Contrast/other DPI/human acceptance remain unverified.

The baseline was reused; native pointer values are compared only within each process and tracked with creation/destruction epochs. The retained-return row indices match (477..494), while the first destination (717..734) and return-to-top have separately observed new construction. EnterImpl/theme-reference sample counts use reliable named ranges; unsupported `??` names stay unresolved. Template calls are not constructor calls, overlapping inclusive sample populations/spans are not added, and a generic folded XamlRootChanged wrapper name is not a fired-event assertion. Zero lost ETW events and narrow QPC/UTC brackets support this selected attribution; they do not eliminate sampling/capture limits.

The public archive excludes full-system ETL/ETLX, PDBs, binaries, symbol/download logs and unrelated process data. It retains selected product frame events, lifetime events for their actual native elements, product UI-thread scheduling/samples, synthetic run traces and all original scroll capture pixels. Exact duplicate PNG bytes are stored once with a hash/path map and can be restored without alteration. Source-system traces and original run directories remain private and untouched.

Disposition: the missing post-69 acquisition is complete, and the cause-backed viewport reduction is implemented with passing scoped behavior guards. Final scroll acceptance remains FAIL; #65 must remain open and Summary held. The early pre-save delays and four uncertain visible delays are the next evidence targets. This record does not diagnose them as checkpoint, GC, framework or SDK defects from overlap alone, and does not recommend a speculative structural change.
