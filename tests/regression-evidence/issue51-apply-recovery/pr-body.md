An interrupted Apply retains its old approval and blocks a new review, even when the user wants to apply their current edits. The confirmation now explains the unfinished fields before selection and offers a direct re-review action. A complete current Project read, reconciliation and withdrawal of the old approval share one checkpoint; verified successes, attempts and pending text survive. No write occurs until the user confirms the new differences.

Validated the recovery and safety guards in logic/storage tests, native UI integration at two window sizes, and an ordinary-app cancellation/recovery journey using an external fake gh. A separate ordinary-app live sandbox journey verified Title, Select set and clear with independent readbacks; its single owned fixture was removed and baseline preservation verified. The user's copied checkpoint also recovered the same 28-row review without remote writes. [Evidence and retained failed attempts](tests/regression-evidence/issue51-apply-recovery/README.md).

Refs #51
Refs #10
