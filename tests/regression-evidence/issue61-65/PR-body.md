## Related Issue

Refs #61. Refs #65. Preserves #64 / draft PR #68; dependent Summary UI remains held for human reevaluation.

## Summary

Boards now routes Actual typing/F2/paste into cumulative dated reports, retains a visible cutoff across rows, and keeps Remaining independent. Contextual date editing uses Auto / explicit datetime, exact native calendar/time controls, and a comparison before adoption. A complete single native assignee plus Project weight removes duplicate per-task assignment; legacy owners, unknown capacity, sparse contributions and historical actuals remain explicit and preserved.

Pending typing no longer invalidates broad table presentation. Detached snapshots and a bounded shared flush preserve newer/reentrant requests, failure retry and close durability. Standard ListView offscreen realization and inactive-editor cleanup are bounded while original pending/IME editors retain identity. No new workspace, scheduler, database, Apply transport or publication path was added.

Both evaluation entry points use the ordinary rebuilt app, source/binary receipts, fresh synthetic roots and isolated credentials. `Start-GanttCheck.ps1` also fixes the reproduced fixture-metadata startup warning by using the common protected launcher. Existing data roots are never overwritten.

## Validation / Testing

Local Windows 11 build 26200, x64, .NET SDK 10.0.401 / runtime 10.0.12, 125% scaling. Final production source: `ad56c06bfd8fbc314d6c92a5bc6692dcbb244dac`; `802004a` changes only the launcher/docs, and `f18a47d` adds evidence. Exact commands, source diffs, binary hashes, data/window conditions, case mapping and retained failures are in [the evidence index](https://github.com/fukuda-yuki/gh-projects-boards/blob/codex/issue-61-65-input-repair/tests/regression-evidence/issue61-65/README.md) and its portable execution ledger.

- **Core: 621 executed / 621 passed / 0 failed / 0 skipped**, final non-live selection. Real workspace/state and isolated storage; external adapters substituted only where required. Additional independent-review defects were observed Red before their fixes: retained-task omission, unknown opposite DATE deletion, empty Actual/history/sparse-contributor loss, assignment and save/reentrant snapshot edges.
- **Actual-control UI integration: final planning/Gantt selection 37/37**, no skips. Production views, native controls and event/binding paths cover Actual routing, typed/native minute order, pending/Undo, settings, and same-task views. The unchanged 80-hop retention check separately passed with zero sampled unloaded editors and original editor/caret retained. Column/row/filter/focus/viewport checks have separate receipts; no aggregate all-green UI run is claimed. Earlier 61/78 and 50/51 results remain, including one unreproduced async teardown timeout and corrected public-control readiness/overflow drivers.
- **Representative ordinary-app journeys: 2/2**, 48 seconds. Setup → weekly actual/remaining/date/dependency work → Gantt → restart → explicit reviewed projections uses isolated fake gh; 1,000-task far-row / narrow-view / Project-switch / restart journey covers lifetime boundaries. This is not live GitHub evidence or a duplicate of every logic matrix.
- **Native diagnostics: cold, warm, physical IME/save failure/retry, and thumb/distant-scroll/horizontal/return each completed 1/1.** On the matched 1,000-task fixture, final title p95 is 52.88/48.21 ms and NUMBER p95 52.47/51.39 ms (cold/warm, UIA observer included). Baseline title 106.29/124.73 ms and NUMBER 118.12/109.66 ms are retained. One run per condition is not repeatability proof. Scroll still has 37/40 callback gaps >=100 ms, maximum 288.21/287.33 ms; independent interior frames confirm unchanged content during delivered input. These freezes remain open, not averaged into a pass.
- **Ordinary launcher check:** user-reported Release + Gantt launch reproduced the metadata warning; corrected launch shows 1,000 tasks without it. Weekly physical F2/digits produced Actual 7 / Remaining 3; normal close and protected Resume read both values back. Fresh PrepareOnly succeeded. Four invalid-root rejection probes preserved original bytes.
- Release solution build passed (two inherited Windows App SDK UI-host CS0436 warnings). Independent frozen-source reviews and every finding's disposition are linked in the evidence index. No contractual test was deleted to obtain a pass.

## Remaining acceptance and delivery

Draft for review and human reevaluation. The earlier human evaluation was negative; a new human evaluation has **not run**. Remaining scroll freezes, other DPI/High Contrast/readout and broader Q1 environment/recovery acceptance remain open. Live GitHub was not rerun; #51/Q2 remains separate. CI results are a separate environment and are not implied by local results. No automatic merge/release or main push; #64 remains intact on its own branch.

